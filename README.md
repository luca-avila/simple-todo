# simple-todo
A simple, production-oriented task manager built with FastAPI and Vue. Supports JWT authentication, group-based task management with roles (owner/member), PostgreSQL persistence, and Dockerized deployment behind Nginx with HTTPS.
